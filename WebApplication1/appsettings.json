{
  "Logging": {
    /*
    * we can Wrap LogLevel to Filter By LogLevelProvider ex :
    then i said form DebugLoggingProvider which is visualt studio debug that is LogLevel
    "Debug": {
        "LogLevel": {
        // Default refer to Project namespace then "Default": "Trace" means from project namespace i need to log from Trace Level
        "Default": "Trace", // if Trace then we said we want trace level and higher severity message
        //the below two lines calls filter logs by category
        "EmployeeManagement.Controllers.HomeController": "Information", // To Detect Log Level from Specific namespace
        "EmployeeManagement.Models.SQLEmployeeRepository": "Trace",
        "Microsoft": "Warning"
      }
    },
    then i said form Other LoggingProvider which is ex:Terminal run bu dotnet run that is LogLevel

        "LogLevel": {
        // Default refer to Project namespace then "Default": "Trace" means from project namespace i need to log from Trace Level
        "Default": "Trace", // if Trace then we said we want trace level and higher severity message
        //the below two lines calls filter logs by category
        "EmployeeManagement.Controllers.HomeController": "Information", // To Detect Log Level from Specific namespace
        "EmployeeManagement.Models.SQLEmployeeRepository": "Trace",
        "Microsoft": "Warning"
      },

    */
    "LogLevel": {
      // Default refer to Project namespace then "Default": "Trace" means from project namespace i need to log from Trace Level
      "Default": "Trace", // if Trace then we said we want trace level and higher severity message
      //the below two lines calls filter logs by category
      "EmployeeManagement.Controllers.HomeController": "Information", // To Detect Log Level from Specific namespace
      "EmployeeManagement.Models.SQLEmployeeRepository": "Trace",
      "Microsoft": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "EmployeeDbConnection": "server=(localdb)\\MSSQLLocalDB;database=EmployeeDB;Trusted_Connection=true"
  }
}
